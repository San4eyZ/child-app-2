import React from 'react';
import PropTypes from 'prop-types';

class Groups extends React.Component {
    render() {
        return (
            <section>
                <h1 className="header1">Группы</h1>
            </section>
        );
    }
}

Groups.propTypes = {};

export default Groups;
